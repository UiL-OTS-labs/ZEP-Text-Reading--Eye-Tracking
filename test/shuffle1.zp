// shuffle1.zp
//
// Script to check the "random" module's item table shuffling function.
// This version is for a one-dimensional item table. For the two-dimensional
// item table (used for between subject's designs) see shuffle2.zp.

import std_defs;

import stimuli;
import random;


start()
{
    load_stimuli();

    shuffle_test_items();
    dump_test_items();

    terminate;
}
